# Session 2026-08-07 — representative ISA (Issue 3)

## Bootstrap
- **Issue:** `PLAN.md` → Issue 3 (ISA operations `{read, write, arith, hash, bin}`).
- **Branch:** `yl-issue-3` off `origin/main-temp` at `b40d8a4`.
- **Read at start:** `README.md`, `INVARIANTS.md`, `CONVENTIONS.md`, `PLAN.md`,
  `CORRESPONDENCE.md`, `STEP_INTERFACES.md`, `LESSONS_LEARNED.md`,
  `paper-digest.md`, `MEMORY_RECONSTRUCTION.md`, the math companion, and the
  pinned ch01/ch03 paper sources.
- **Build at start:** `lake build` green (8,591 jobs) at `b40d8a4`.

## What changed
- `Memory.lean`: generalized `FullMemory.read` and `FullMemory.write` over
  memory index/value types without changing their equations. This lets the ISA
  reuse them for the paper's `Addr → Byte` memory instead of copying them.
- `ISA.lean`: added the five representative operation classes, fixed code and
  per-operation PC/register predicates, the full per-operation predicate with
  fetch and memory equations, the five-way plain step, and the memory-preservation
  theorem.
- `ISASanity.lean`: added private accepting read/changed-write examples,
  wrong-fetch and incorrect-write rejection checks, and a private `ZkVM` whose
  `step` field is `ISA.System.stepPlain`.
- Updated the umbrella import and the living README, correspondence matrix,
  step-interface note, and math companion.
- New public surface (I5): `ISA.OperationClass`, `ISA.System`,
  `ISA.System.operation`, `ISA.System.stepPlain`, and
  `ISA.System.operation_preserves_memory_unless_write`. The apparent fifth
  item is the required result over the four definition-level surfaces; no
  separate binary execution relation was introduced.

## Axiom / `sorry` ledger diff
- `#print axioms ISA.System.operation_preserves_memory_unless_write`: `[propext]`
  (permitted by I7).
- `sorry`/`sorryAx`/`admit` added: none.
- Repository-wide hygiene audit: green across 563 declarations.

## CORRESPONDENCE rows touched
- Added Issue 3 rows for `ISA.OperationClass`, `ISA.System`,
  `ISA.System.operation`, `ISA.System.stepPlain`, and the non-write
  memory-preservation theorem; all are proved and await George's human fidelity
  review.
- Removed the now-implemented ISA row from the planned table.
- Clarified the boundary between the plain ISA equations and Issue 5's future
  conjunction with committed `MemStep`/bus evidence.

## Adversarial review
- Dimensions run: (a) vacuity, (b) fixed-program fetch, (c) memory mutation,
  (d) ensuring there is only one plain `ZkVM.step`, (e) CI/docs, and
  (f) prospective Issue 2 overlap.
- CONFIRMED findings: the Issue 2 and Issue 3 sources merge and build together;
  both branches append to `math-companion.md`, producing one documentation-only
  conflict that must be resolved by retaining and ordering both sections.
- SUSPECTED: none.
- The combined disposable-clone build is green (8,594 jobs). No injected probe
  touched the real worktree; its status contains only the intended Issue 3 diff.

## Build at end
- `lake build`: green (8,593 jobs).
- `ci_checks.py --self-test --check-correspondence --check-axioms --check-hygiene`:
  green; all 38 audited correspondence declarations elaborate.

## Handoff note
- George must confirm that the five-class abstraction is the intended
  representative simplification, that every operation contains the fetch
  condition, and that `stepPlain` is exactly the required disjunction.
- This issue defines the plain `R*` target semantics. Issue 5 still owns the
  committed/bus predicate that associates an extracted `MemStep` witness with
  the selected operation; Issue 7 owns the public assembled `ZkVM` instance.
